import { View, Text } from 'react-native'

export default function JobsScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Modulo jobs — da implementare</Text>
    </View>
  )
}
