import { View, Text } from 'react-native'

export default function TrackingScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Modulo tracking — da implementare</Text>
    </View>
  )
}
